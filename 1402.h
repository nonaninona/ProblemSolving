#include <iostream>
using namespace std;

int T, A, B;

int main_1402() {
	ios::sync_with_stdio(0);
	cin.tie(0); cout.tie(0);

	cin >> T;
	for (int i = 0; i < T; i++) {
		cin >> A;
		cin >> B;
		cout << "yes" << '\n';
	}

	return 0;
}