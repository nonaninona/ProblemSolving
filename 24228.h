#include <iostream>
#define ll long long
using namespace std;

ll N, R;

int main_24228() {
	ios::sync_with_stdio(0);
	cin.tie(0); cout.tie(0);

	cin >> N;
	cin >> R;
	cout << N + (R*2 - 1);

	return 0;
}